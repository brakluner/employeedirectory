import React from "react";

function Toast(props) {

        return (
            <div>Name:{props.title}&nbsp;
                 {props.first}&nbsp;
                 {props.last}</div>
        );
        };
export default Toast;